package main

func main() {
	// db, err := db.Connect()
	// if err != nil {
	// 	log.Fatal("Failed to connect to Redis:", err)
	// }

	// router := gin.Default()
	// router.GET("/search/:query", func(c *gin.Context) {
	// 	query := c.Param("query")

	// 	c.JSON(200, gin.H{
	// 		"message": query,
	// 	})
	// })
	// router.Run(":5000")
}
